1) Hide them (no text)
2) write click event:
    Click --> retrieve the HTML element's `.dataset.value` for innerText
    
    if matching != null:
        matching = dataSetValue
    else:
        if matching == dataSetValue:
            //they are matching!

matching = null;

